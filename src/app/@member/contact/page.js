import { ContactForm } from '@/app/contact/form';

export default function PageContactVisitor() {
  return (
    <div className='flex flex-col grow justify-center items-center'>
      <ContactForm />
    </div>
  );
}